import MatchingEngine

// matchbook — driver / server entry point.
//
// Phase 1: build a scripted order-sequence driver here to exercise the engine
//          (submit a hardcoded list of orders, print resulting trades + book).
// Phase 3: this becomes the Network.framework TCP server.
//
// Replace the line below as you go.

print("matchbook \(MatchingEngine.version) — see SPEC.md to start Phase 1")
